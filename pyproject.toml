[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "gadget"
version = "0.1.0"
description = "A basic gadget project"
readme = "README.md"
requires-python = ">=3.8"
dependencies = []

[project.scripts]
gadget = "gadget.cli:main"
